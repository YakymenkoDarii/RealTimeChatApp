Real-Time Chat Application

Це повностековий веб-додаток, створений на .NET 8 та Angular, який демонструє обмін повідомленнями в реальному часі, автентифікацію та інтеграцію з хмарними сервісами Azure.

Додаток використовує Azure SignalR Service для масштабування WebSocket-з'єднань та Azure Cognitive Services для аналізу тональності (sentiment analysis) повідомлень.

Live Demo: https://real-time-chat-task.azurewebsites.net

🚀 Основні Можливості

Автентифікація: Повна реєстрація користувачів та вхід в систему з використанням JWT (JSON Web Tokens).

Чат у реальному часі: Миттєвий обмін повідомленнями між користувачами за допомогою SignalR.

Список контактів: Динамічний список користувачів, що показує їхній онлайн/офлайн статус.

Індикатор набору: Користувачі бачать, коли їхній співрозмовник друкує повідомлення.

Аналіз тональності: (Додатково) Автоматичне визначення емоційного забарвлення ("positive", "negative", "neutral") кожного повідомлення за допомогою Azure Text Analytics API.

Завантаження історії: Повідомлення зберігаються в базі даних і завантажуються при відкритті чату, з підтримкою нескінченної прокрутки (infinite scroll) для завантаження старих повідомлень.

CI/CD: Проєкт налаштований на автоматичне розгортання в Azure App Service при кожному push-у гілку master за допомогою GitHub Actions.

🛠️ Технологічний стек

Backend (.NET)

.NET 8 (C#)

ASP.NET Core Web API

Entity Framework Core 8

ASP.NET Core Identity (для керування користувачами)

SignalR

Frontend (Angular)

Angular 17+

TypeScript

RxJS

Хмара та База даних (Azure)

Azure App Service: Хостинг для веб-додатку.

Azure SQL Database: Робоча (production) база даних.

Azure SignalR Service: Масштабована служба для обробки WebSocket-з'єднань.

Azure Cognitive Services (Language): Cервіс для аналізу тональності повідомлень.

GitHub Actions: Для автоматизованого CI/CD.

💻 Налаштування та запуск локально

Для запуску проєкту на локальній машині вам знадобиться Visual Studio 2022 (з .NET 8 SDK) та Node.js.

1. Клонування репозиторію

git clone [https://github.com/YakymenkoDarii/RealTimeChatApp.git](https://github.com/YakymenkoDarii/RealTimeChatApp.git)
cd RealTimeChatApp


2. Конфігурація C# Сервера

Проєкт використовує appsettings.Development.json для локальних налаштувань. Вам потрібно надати власні ключі та рядки підключення.

Відкрийте файл: RealTimeChatApp.Server/appsettings.Development.json

{
  "ConnectionStrings": {
    // 1. Вкажіть ваш локальний рядок підключення до MS SQL Server
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=ChatAppDb;Trusted_Connection=True;MultipleActiveResultSets=true"
  },
  // 2. Вкажіть ваші ключі для Azure AI (сентимент-аналіз)
  // (Або закоментуйте сервіс у Program.cs та ChatHub.cs, якщо не тестуєте цю функцію)
  "CognitiveServices": {
    "Endpoint": "YOUR_AZURE_LANGUAGE_SERVICE_ENDPOINT",
    "Key": "YOUR_AZURE_LANGUAGE_SERVICE_KEY"
  },
  // 3. Вкажіть будь-який довгий, секретний рядок для JWT
  "Jwt": {
    "Issuer": "https://localhost:7107", // (Це стандартна локальна адреса)
    "Audience": "https://localhost:7107",
    "IssuerSigningKey": "GENERATE_A_NEW_LONG_SECRET_KEY_HERE" 
  }
}


3. Створення локальної бази даних

Після оновлення DefaultConnection, створіть таблиці у вашій локальній базі даних.

У Visual Studio відкрийте Tools > NuGet Package Manager > Package Manager Console.

Переконайтеся, що RealTimeChatApp.Server обрано як "Default project".

Виконайте команду:

Update-Database


4. Конфігурація Angular Клієнта

Переконайтеся, що клієнт знає адресу локального сервера.

Відкрийте файл: realtimechatapp.client/src/environments/environment.ts

export const environment = {
  production: false,
  // Ця URL має збігатися з 'applicationUrl' у 'launchSettings.json'
  apiUrl: 'https://localhost:7107' 
};


5. Запуск

Відкрийте .sln файл у Visual Studio.

Оберіть RealTimeChatApp.Server як початковий проєкт.

Оберіть профіль запуску httpss (не http).

Натисніть F5 або зелену кнопку "Start".

Visual Studio автоматично запустить C# бекенд та Angular SPA Proxy, виконає npm install і відкриє браузер за адресою https://localhost:4200.
